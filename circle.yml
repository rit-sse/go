machine:
  node:
    version: 0.12.0

dependencies:
  post:
    - npm install eslint@2.2.0 #the cached version doesn't work

# We'll write tests one day...for now we'll just run the linter
test:
  override:
    - npm run lint
    - npm run build

deployment:
  master:
    branch: master
    owner: rit-sse
    commands:
      - ssh deploy@$SSE_HOST 'cd /docker && docker-compose restart go nginx'
